# v0.5.0.2

## 启动动画

- **眼睛眨眼动画**：替换原狐狸 logo，使用 app icon 中的黑色椭圆眼睛作为启动页动画
- **动画效果**：眼睛淡入 → 左右眼先后各眨一下（错开 120ms）→ 跳转仪表盘，总时长 1 秒

## Lint 修复

- **UseAppTint**：`android:tint` → `app:tint`（activity_dashboard.xml，2 处）
- **MissingTranslation**：补充 18 条中文字符串到 `values-zh/strings.xml`
- **lint.xml 清理**：移除无效的 Kotlin-specific issue ID，仅保留有效 Android lint 抑制规则

## CI 优化

- lint 任务移除 `continue-on-error: true`，lint 失败将阻断构建
