# v0.5.0.2

## Splash Animation

- **Eyes Blink Animation**: Replaced fox logo with black oval eyes from app icon as splash screen animation
- **Animation Effect**: Eyes fade in → left and right eyes blink sequentially (staggered 120ms) → navigate to dashboard, total duration 1 second

## Lint Fixes

- **UseAppTint**: Changed `android:tint` → `app:tint` in activity_dashboard.xml (2 occurrences)
- **MissingTranslation**: Added 18 missing Chinese strings to `values-zh/strings.xml`
- **lint.xml Cleanup**: Removed invalid Kotlin-specific issue IDs, kept only valid Android lint suppression rules

## CI Improvements

- Removed `continue-on-error: true` from lint job; lint failures now block the build
